{
  "version": "1.0.0",
  "license": "Apache-2.0",
  "name": "@trustchair/metakey-client-js",
  "author": "Trustchair Ltd",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build:ts",
    "build:ts": "tsc -b .",
    "prebuild": "npm run clean",
    "clean": "rimraf -rf dist coverage docs *.tsbuildinfo",
    "lint": "tslint --project .",
    "lint:fix": "tslint --project . --fix",
    "precommit": "lint-staged",
    "docs": "typedoc --out ./docs ./src/index.ts --excludeInternal"
  },
  "tslint": {
    "extends": "./tslint.json"
  },
  "dependencies": {},
  "devDependencies": {
    "lint-staged": "^11.0.0",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typedoc": "^0.20.36",
    "typescript": "^4.3.2"
  }
}
